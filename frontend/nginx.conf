server {
    listen 80;
    server_name _;

    # Public requests are logged by the gateway with query strings removed.
    # Do not duplicate potentially sensitive URLs in the internal static server.
    access_log off;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
